# Internal docs

Notes that explain *why* a QA profile does what it does, per-repo history of
flaky flows, and anything else you would not publish. `/loop-qa` never reads
this directory; it is for humans and for the agent helping you maintain the
profiles.
